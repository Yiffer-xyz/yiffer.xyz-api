version: "3"

services:
  server:
    image: cliserv:latest
    ports:
      - "8012:8012"
    networks:
      - redis-net

  redis:
    image: redis:alpine
    ports:
      - "6379:6379"
    command: redis-server --appendonly yes
    networks:
      - redis-net

networks:
  redis-net:
